import {useWorkshop} from './store';import {boardColumns} from './shared/grouping';import {adjacentStatus,boardLimitWarning} from './shared/board';import {filterTasks} from './shared/filters';
export default function Board(){const {state,filters,navigate,dispatch}=useWorkshop();return <section aria-label="Status board"><h2>Work in motion</h2><p>Move work with buttons; every transition uses the same completion rules as the table.</p><div className="board">{boardColumns(filterTasks(state,filters).filter(task=>state.preferences.showCompleted||task.status!=='done')).map(column=><section className="board-column" key={column.status}><h3>{column.label} <span className="count">{column.tasks.length}</span></h3><p className="muted">{column.points} points</p>{column.status==='doing'&&boardLimitWarning(state,column.status,3)&&<p className="notice">{boardLimitWarning(state,column.status,3)}</p>}{column.tasks.map(task=><article className="board-task" key={task.id}><button className="text-button" onClick={()=>navigate('tasks',task.id)}>{task.title}</button><p><small>{task.id} • {task.priority} • {task.estimate} points</small></p><div className="row"><button aria-label={'Move '+task.title+' backward'} disabled={!adjacentStatus(task.status,-1)} onClick={()=>dispatch('task.move',{id:task.id,direction:-1})}>Back</button><button aria-label={'Move '+task.title+' forward'} disabled={!adjacentStatus(task.status,1)} onClick={()=>dispatch('task.move',{id:task.id,direction:1})}>Next</button></div></article>)}{!column.tasks.length&&<p className="empty">No work in this stage.</p>}</section>)}</div></section>;}
