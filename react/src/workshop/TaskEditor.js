import {useState} from 'react';import {useWorkshop} from './store';import {blankTask} from './shared/create-task';import {PRIORITIES} from './shared/types';import {TASK_TEMPLATES,taskFromTemplate} from './shared/templates';
export default function TaskEditor({task,onClose}){const {state,dispatch}=useWorkshop();const [draft,setDraft]=useState(()=>task?{...task}:blankTask(state));const update=(key,value)=>setDraft({...draft,[key]:value});function save(event){event.preventDefault();if(dispatch(task?'task.edit':'task.create',task?{id:task.id,values:draft}:draft))onClose();}return <section className="panel" aria-labelledby="editor-title"><h2 id="editor-title">{task?'Edit task':'New task'}</h2>{!task&&<label>Start from a template<select defaultValue="" onChange={event=>setDraft(taskFromTemplate(state,event.target.value))}><option value="">Blank task</option>{TASK_TEMPLATES.map(template=><option key={template.id} value={template.id}>{template.name}</option>)}</select></label>}<form onSubmit={save} noValidate className="stack"><label>Task title<input autoFocus value={draft.title} onChange={event=>update('title',event.target.value)} maxLength={120} required/></label><label>Description<textarea value={draft.description} onChange={event=>update('description',event.target.value)} rows={3}/></label><div className="row"><label>Project<select value={draft.projectId} onChange={event=>update('projectId',event.target.value)}>{state.projects.map(project=><option key={project.id} value={project.id}>{project.name}</option>)}</select></label><label>Owner<select value={draft.assignee} onChange={event=>update('assignee',event.target.value)}><option value="">Unassigned</option>{state.members.map(member=><option key={member.id} value={member.id}>{member.name}</option>)}</select></label></div><div className="row"><label>Priority<select value={draft.priority} onChange={event=>update('priority',event.target.value)}>{PRIORITIES.map(priority=><option key={priority}>{priority}</option>)}</select></label><label>Due date<input type="date" value={draft.dueDate} onChange={event=>update('dueDate',event.target.value)}/></label><label>Estimate points<input type="number" min="0" max="100" step="1" value={draft.estimate} onChange={event=>update('estimate',Number(event.target.value))}/></label></div><p className="muted">Title needs 3–120 characters. Estimates use whole points from 0 to 100.</p><div className="row"><button className="primary" type="submit">{task?'Save task':'Create task'}</button><button type="button" onClick={onClose}>Cancel</button></div></form></section>;}
