import {screen,fireEvent,within} from '@testing-library/react';
import {renderWorkshop,storedWorkspace,changeRoute,openTask} from './testHelpers';

test('selection enables bulk owner changes and clear removes the toolbar',async()=>{const {user}=renderWorkshop();await user.click(screen.getByRole('checkbox',{name:'Select Document recovery objectives'}));await user.click(screen.getByRole('checkbox',{name:'Select Automate database restore drill'}));expect(screen.getByRole('heading',{name:'2 tasks selected'})).toBeInTheDocument();await user.selectOptions(screen.getByLabelText('Bulk owner'),'noor');await user.click(screen.getByRole('button',{name:'Assign owner'}));expect(storedWorkspace().tasks.filter(task=>['T-101','T-102'].includes(task.id)).every(task=>task.assignee==='noor')).toBe(true);await user.click(screen.getByRole('button',{name:'Clear selection'}));expect(screen.queryByLabelText('Selected task actions')).not.toBeInTheDocument();});
test('select matching spans all filtered pages',async()=>{const {user}=renderWorkshop();await user.click(screen.getByRole('button',{name:'Select matching tasks'}));expect(screen.getByRole('heading',{name:'12 tasks selected'})).toBeInTheDocument();await user.click(screen.getByRole('button',{name:'Deselect matching tasks'}));expect(screen.queryByRole('heading',{name:/tasks selected/})).not.toBeInTheDocument();});
