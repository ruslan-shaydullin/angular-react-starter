import {useState} from 'react';import {useWorkshop} from './store';import {taskRelations,dependencyCandidates} from './shared/relations';
export default function Dependencies({task}){const {state,dispatch,navigate}=useWorkshop();const [candidate,setCandidate]=useState('');const relations=taskRelations(state,task.id);return <section className="detail-section"><h3>Dependencies</h3><ul className="plain-list">{relations.dependencies.map(item=><li className="row" key={item.id}><button className="text-button" onClick={()=>navigate('tasks',item.id)}>{item.title}</button><small>{item.status}</small><button aria-label={'Remove dependency '+item.title} onClick={()=>dispatch('task.dependencies',{id:task.id,ids:task.dependsOn.filter(id=>id!==item.id)})}>Remove</button></li>)}</ul><form className="row" onSubmit={event=>{event.preventDefault();if(candidate&&dispatch('task.dependencies',{id:task.id,ids:[...task.dependsOn,candidate]}))setCandidate('');}}><label>Add dependency<select value={candidate} onChange={event=>setCandidate(event.target.value)}><option value="">Choose a task</option>{dependencyCandidates(state,task.id).filter(item=>!task.dependsOn.includes(item.id)).map(item=><option key={item.id} value={item.id}>{item.title}</option>)}</select></label><button disabled={!candidate||task.archived}>Add dependency</button></form>{relations.dependents.length>0&&<p>Needed by: {relations.dependents.map(item=>item.id).join(', ')}</p>}</section>;}
