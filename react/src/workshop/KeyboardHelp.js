import {useEffect} from 'react';import {useWorkshop} from './store';import {keyboardCommand,SHORTCUTS} from './shared/keyboard';
export default function KeyboardHelp(){const {help,setHelp,navigate,setEditing,undo,redo}=useWorkshop();useEffect(()=>{function keydown(event){const command=keyboardCommand({...event,key:event.key,ctrlKey:event.ctrlKey,metaKey:event.metaKey,shiftKey:event.shiftKey,altKey:event.altKey,targetTag:event.target.tagName,contentEditable:event.target.isContentEditable});if(!command)return;if(command==='search'){navigate('tasks');setTimeout(()=>document.getElementById('task-search')?.focus(),0);}if(command==='new'){navigate('tasks');setEditing(true);}if(command==='help')setHelp(value=>!value);if(command==='undo')undo();if(command==='redo')redo();event.preventDefault();}window.addEventListener('keydown',keydown);return()=>window.removeEventListener('keydown',keydown);},[navigate,setEditing,setHelp,undo,redo]);return <section className="keyboard-help"><button aria-expanded={help} onClick={()=>setHelp(!help)}>Keyboard help</button>{help&&<div className="panel"><h2>Keyboard shortcuts</h2><p>Single-key shortcuts are inactive while editing a text field.</p><dl>{SHORTCUTS.map(shortcut=><div className="row" key={shortcut.keys}><dt><kbd>{shortcut.keys}</kbd></dt><dd>{shortcut.action}</dd></div>)}</dl></div>}</section>;}
